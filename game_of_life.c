#include <stdio.h>

#define SIZE 100

void generate_next_step(int grid[SIZE][SIZE]);

int main()
{
	int i, j;
	int grid[SIZE][SIZE];

	for (i = 0; i < SIZE; i++)
		for (j = 0; j < SIZE; j++)
			grid[i][j] = 0;
}

void generate_next_step(int grid[SIZE][SIZE])
{
	;
}
